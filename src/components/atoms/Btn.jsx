export const Btn = ({title}) => {
    return (
        <button className="bg-cyan-700 absolute top-10 left-3 p-2 border-solid rounded-xl text-white font-black">
            {title}
        </button>
    )
}